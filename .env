# .env - store secrets locally (do NOT commit to VCS)
# Replace the placeholder below with your WandB API key (exactly 40 characters)
# Example: WANDB_API_KEY=abcd0123efgh4567ijkl8901mnop2345qrstuv
# After editing, verify length in PowerShell:
#   (Get-Content .env).Split('=')[1].Trim().Length  # should output 40

WANDB_API_KEY=YOUR_40_CHAR_WANDB_KEY_HERE